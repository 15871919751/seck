<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ct.seckill.dao.GoodsDao">

    <select id="getListGoodsVo" resultType="org.ct.seckill.vo.GoodsVo">
     SELECT g.*,
     mg.seckill_price,mg.stock_count,mg.start_date,mg.end_date
     FROM miaosha_goods mg LEFT JOIN goods g ON mg.goods_id=g.id
     </select>

    <select id="getGoodsVoByGoodsId" resultType="org.ct.seckill.vo.GoodsVo">
     SELECT g.*,
     mg.seckill_price,mg.stock_count,mg.start_date,mg.end_date
     FROM miaosha_goods mg LEFT JOIN goods g ON mg.goods_id=g.id WHERE g.id=#{goodsId}
    </select>

    <update id="reduceStock" parameterType="org.ct.seckill.domain.MiaoshaGoods">
        UPDATE miaosha_goods set stock_count=stock_count-1 where goods_id=#{miaoshaGoods.goodsId} and stock_count>0
    </update>

</mapper>